{
  "name": "sn-videos",
  "version": "1.0.0",
  "description": "App Android - Generador de videos SN (Salta News). Sin depender de la Mac.",
  "capacitor": {
    "platforms": ["android"]
  },
  "scripts": {
    "build": "echo 'No build step - web is remote'",
    "cap:android": "npx cap open android",
    "sync": "npx cap sync android"
  },
  "devDependencies": {
    "@capacitor/android": "^6.0.0",
    "@capacitor/cli": "^6.0.0"
  },
  "dependencies": {
    "@capacitor/core": "^6.0.0"
  }
}
